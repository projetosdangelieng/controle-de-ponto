# Copie este arquivo para ".streamlit/secrets.toml" para rodar localmente,
# OU (recomendado) configure estes mesmos valores em:
# https://share.streamlit.io -> seu app -> Settings -> Secrets
#
# NUNCA suba o arquivo "secrets.toml" de verdade para o GitHub (ele já está
# no .gitignore). Este arquivo .example serve apenas de modelo.

TURSO_DATABASE_URL = "libsql://controle-de-ponto-dangeli.aws-us-east-1.turso.io"
TURSO_AUTH_TOKEN = "cole_aqui_o_token_gerado_no_turso"
